const styles = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: 50,
    fontFamily: 'Arial, sans-serif',
    fontSize: 32,
}


function Logo() {
    return (
        <div style={styles}>
            Logo
        </div>
    );
}

export default Logo;
